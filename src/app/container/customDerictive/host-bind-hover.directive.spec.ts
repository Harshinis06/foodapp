import { HostBindHoverDirective } from './host-bind-hover.directive';

describe('HostBindHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new HostBindHoverDirective();
    expect(directive).toBeTruthy();
  });
});
